class FraudDetails {
  FraudDetails();

  factory FraudDetails.fromJson(Map<String, dynamic> json) {
    throw UnimplementedError('FraudDetails.fromJson($json) is not implemented');
  }

  Map<String, dynamic> toJson() {
    throw UnimplementedError();
  }
}
